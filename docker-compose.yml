version: '3.8'

services:
  godot-tower-defense:
    build: .
    container_name: godot-tower-defense
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
